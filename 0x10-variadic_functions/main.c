#include "variadic_functions.h"

/**
 * main - check the code
 *
 * Return: Always 0.
 */
int main(void)
{
    print_all(NULL, 'B', 3, "stSchool", .1);
	print_all("cisf", 'B', 3, "stSchool", .1);
	 
	return (0);
}
